import React from 'react';

const Item = ({ department}) => {
  return "hello";

  {/* <>

      /* key={department.id}
      {props.department.name}
    <ul>
      {item.name}
       {item.description}
      {item.price}
     </ul>
  </>
 }
   */}
}

export default Item;